# Project Rules

## Do Not Run Tests

- Do not run automated tests in this project.
- Do not use `php artisan test`, `phpunit`, `npm test`, or similar test commands.
- Prefer manual verification and code inspection instead.

## Working Agreement

- Treat this rule as a default for all future work in this repository unless the user explicitly changes it.
